{
  "name": "betfactor-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "BetFactor live odds scraper",
  "main": "dist/index.js",
  "scripts": {
    "postinstall": "prisma generate --schema=./prisma/schema.prisma || true",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node --project tsconfig.json index.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.3.2",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "rimraf": "^5.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
